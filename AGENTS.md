# Byo-yomi Web Agent Instructions

## このリポジトリについて

- 株式市場の寄り・引けをカウントダウンし、残り10秒から秒読み音声を鳴らすPWA。
- 主な実装は `index.html` にまとまっている。PWA設定は `manifest.json`、オフラインキャッシュは `sw.js`。
- Android版は別リポジトリ `marunwonderland/byo-yomi-and`。必要な場合のみ参考にし、このWeb版へAndroid固有実装を持ち込まない。

## 調査範囲

- 最初からリポジトリ全体を探索しない。
- タスクに直接関係するファイルから確認し、必要な依存先だけ1階層ずつ広げる。
- UI・カウントダウン・市場時刻・音声処理の変更は、まず `index.html` を確認する。
- PWAインストール・メタデータの変更は `manifest.json`、キャッシュやオフライン動作は `sw.js` を確認する。
- 明示的な必要性がない限り、Electron化・Capacitor化などREADME記載の将来案を調査・実装しない。

## 変更方針

- ユーザーが依頼した範囲だけ変更する。
- 明示的な依頼なしに、リファクタ、ファイル分割、フレームワーク導入、依存追加、命名変更、コード整形を行わない。
- 単一HTML構成を「改善目的」で勝手に分割しない。
- 既存の市場時刻、表示、PWA、音声動作を保つ。
- Android版との共通化を理由にWeb版の設計を変更しない。

## 秒読み・バックグラウンド動作

- 現在の秒読みは、通常の `setInterval` / `setTimeout` だけに依存せず、Web Audio APIのタイムラインへ音声を事前スケジューリングすることでバックグラウンドタブのタイマースロットリングに耐える設計。
- Page Visibility APIでタブ復帰時に時刻と音声スケジュールを再同期する。
- この仕組みを、単純な1秒タイマー中心の実装へ戻さない。
- 表示更新用タイマーと音声再生タイミングを同一の精度前提で扱わない。表示が遅延しても、音声のタイミング精度を落とさない。
- 音声関係の不具合では、まず現在時刻・対象市場時刻・Web Audioのスケジュール・Visibility復帰処理のどこがずれているかを切り分ける。
- OSスリープ、ブラウザによるタブ破棄（Discard）などWebアプリから制御できないケースを、コード上の無限リトライや常駐処理で無理に回避しない。

## 市場時刻

- 寄り・引け・セッション時刻の変更では、対象市場とタイムゾーンを明示して確認する。
- ローカル時刻の文字列比較や固定UTCオフセットだけで処理せず、既存の時刻計算方式を確認してから変更する。
- NYSE / NASDAQと東京市場のロジックを一括置換しない。変更対象の市場だけを狭く修正する。
- 祝日・サマータイム等を新たに扱う場合は、それを別仕様として明示し、既存タスクへ勝手に含めない。

## PWA / Service Worker

- `sw.js` の変更はキャッシュ済み旧版ユーザーへの影響を考慮する。
- UIだけの変更で不要にService Workerやmanifestを触らない。
- キャッシュ更新問題を直す場合、キャッシュキー・対象ファイル・更新タイミングを確認し、単にキャッシュ全削除へ逃げない。

## 確認

- 変更後は、タスクに直接関係する最小範囲を確認する。
- 秒読み変更では最低限、通常表示と残り10秒の音声開始ロジックを確認する。
- バックグラウンド関連変更では、可能ならタブを非表示にした状態と復帰後の再同期を確認する。
- PWA変更では、必要な場合だけmanifest / Service Workerの登録・更新を確認する。
- OSスリープやブラウザDiscardなど再現困難な範囲を、確認済みと報告しない。

## 報告

完了報告は簡潔に以下だけを示す。

1. 原因または実装方針
2. 変更したファイル
3. 実施した確認と結果
4. ブラウザ・OS制約など残る問題（ある場合のみ）

調査過程や読んだファイルを網羅的に列挙しない。
